# Revised implementation sequence

1. **Resolve shared launch blockers before page work.** Confirm staging robots behavior, canonical/social metadata strategy, missing home logo, missing leaflet, and internal-route-check treatment of generated/static resources. This prevents each migrated page inheriting a known defect.
2. **Set the Sustainability information architecture.** Decide the canonical roles of `/green-steel` (education) and `/ars-green-steel` (brand/product proof), then approve the supporting evidence plan for embodied-carbon and certification claims. This prevents duplicate intent and rework.
3. **Confirm product and certification inputs.** Obtain Binders product data/assets and SGBC/other proof files. These are blockers, not development tasks.
4. **Implement Vision & Mission in About.** It uses an existing anchor and has no route or asset dependency; it also resolves an advertised header destination.
5. **Update the three existing Project Type routes as one template stream.** Road Projects, Bridges & Flyovers, and Institutional Projects share page anatomy, navigation changes, product proof and conversion logic. Retain their URLs.
6. **Reconcile the 13 existing implementations.** Work from `implemented-pages-audit.md`, beginning with shared Home/Product assets, then the audience template, then individual About/Product pages. Do not call any page complete until source sections and assets are reconciled.
7. **Add leadership messages after sign-off.** Apply only after quote/attribution/portrait clearance; keep it within Leadership.
8. **Build Binders after product approval.** Add the new route, product menu/footer references, metadata, image set and brochure together.
9. **Execute SEO and responsive QA across every changed route.** Validate one canonical URL, metadata/social images, internal links, assets, desktop plus real mobile devices, menu/footer links, and conversion CTAs. Staging remains noindex until launch approval.
